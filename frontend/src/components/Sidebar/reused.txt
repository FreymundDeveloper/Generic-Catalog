import React from 'react';
import styled from 'styled-components';

export const Modal = ({ isOpen, description, seller, onClose }) => {
  return (
    <ModalWrapper isOpen={isOpen}>
      <ModalContent>
        <CloseButton onClick={onClose}>&times;</CloseButton>
        <SellerTitle>{seller}</SellerTitle>
        <Description>{description}</Description>
      </ModalContent>
    </ModalWrapper>
  );
};

const ModalWrapper = styled.div`
  display: ${props => (props.isOpen ? 'flex' : 'none')};
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
`;

const ModalContent = styled.div`
  background: #130c0c;
  width: 50%;
  max-width: 400px;
  padding: 20px;
  text-align: left; /* Alinhar o texto à esquerda */
`;

const CloseButton = styled.span`
  position: absolute;
  top: 10px;
  right: 10px;
  cursor: pointer;
  color: white;
`;

const SellerTitle = styled.h2`
  font-size: 1.5rem; /* Ajuste o tamanho do título conforme necessário */
  margin-bottom: 10px; /* Adicione espaço entre o título e a descrição */
  color: white;
`;

const Description = styled.p`
  color: white;
`;
